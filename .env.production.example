# ===========================================
# One Health CMS - Production Environment
# ===========================================
# Copy this file to .env and fill in the values

# Domain
DOMAIN=onehealth.cm

# GitHub (for pulling images)
GITHUB_REPOSITORY=your-username/onehealth-cms

# Database
DB_ROOT_PASSWORD=your-secure-root-password
DB_NAME=onehealth_cms
DB_USER=onehealth_user
DB_PASSWORD=your-secure-db-password

# JWT Secret (generate with: openssl rand -base64 64)
JWT_SECRET=your-super-secret-jwt-key-at-least-64-characters-long

# CORS Origins (comma-separated, optional - defaults are set in server.js)
# CORS_ORIGINS=https://onehealth.cm,https://www.onehealth.cm,https://admin.onehealth.cm

# Email Configuration (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_FROM=noreply@onehealth.cm

# Let's Encrypt Email (for SSL certificate notifications)
LETSENCRYPT_EMAIL=admin@onehealth.cm

# ===========================================
# DO NOT commit this file with real values!
# ===========================================
